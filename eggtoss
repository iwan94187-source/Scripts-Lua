
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local runService = game:GetService("RunService")

local eggvalue = nil

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local remoteEvents = ReplicatedStorage:WaitForChild("RemoteEvents")
local eggThrowEvent = remoteEvents:WaitForChild("EggThrowEvent")
local didCatchEvent = remoteEvents:WaitForChild("DidCatch")
local StepBackEvent = remoteEvents:WaitForChild("StepBack")

-- 1. إنشاء الواجهة والخط الرئيسي (Slider Frame)
local screenGui = Instance.new("ScreenGui", PlayerGui)
screenGui.ResetOnSpawn = false

local blk_color, gry = Color3.fromRGB(24,24,24), Color3.fromRGB(52, 52, 52)

local helperTextLabel = Instance.new("TextLabel")
helperTextLabel.Size=UDim2.new(0,200,0,50)
helperTextLabel.Transparency=0.5
helperTextLabel.BackgroundColor3=blk_color
helperTextLabel.TextColor=BrickColor.new("White")
helperTextLabel.TextScaled=true
helperTextLabel.Parent=screenGui

local function printlog(txt,set)
	if set then 
		helperTextLabel.TextColor=BrickColor.new("Really red")
	else
		helperTextLabel.TextColor=BrickColor.new("Lime green")
	end
	helperTextLabel.Text=txt
end

local eggvalue = LocalPlayer:WaitForChild("otherLeaderstats"):WaitForChild("Eggs")
local coinValue = LocalPlayer:WaitForChild("otherLeaderstats"):WaitForChild("Coins")

printlog("Egg found",false)

local Mainframe = Instance.new("Frame")
Mainframe.Size=UDim2.new(0,250,0,250)
Mainframe.Position=UDim2.new(0.125,0,0,0)
Mainframe.BackgroundColor3=Color3.fromRGB(24, 24, 24)
Mainframe.Visible=false
Mainframe.Active=true
Mainframe.Draggable=true
Mainframe.Parent=screenGui

local frameTitle = Instance.new("TextLabel")
frameTitle.Size=UDim2.new(1,0,0.1,0)
frameTitle.Text="   Egg Toss V7.0"
frameTitle.TextXAlignment=Enum.TextXAlignment.Left
frameTitle.TextColor=BrickColor.new("White")
frameTitle.BackgroundColor3=blk_color
frameTitle.TextSize=7
frameTitle.Parent=Mainframe

local iconFrame = Instance.new("ImageButton")
iconFrame.Size=UDim2.new(0,40,0,40)
iconFrame.Position=UDim2.new(0.3,0,0.3,0)
iconFrame.BackgroundColor3=Color3.fromRGB(24, 24, 24)
iconFrame.Image = "rbxassetid://85847690672478"
iconFrame.Active=true
iconFrame.Draggable=true
iconFrame.Parent=screenGui

local destroyBtn = Instance.new("TextButton")
destroyBtn.Size=UDim2.new(0,22.5,0,22.5)
destroyBtn.Position=UDim2.new(0.915,0,0,0)
destroyBtn.BackgroundColor3=Color3.fromRGB(255, 0, 0)
destroyBtn.TextColor=BrickColor.new("White")
destroyBtn.Text="x"
destroyBtn.Parent=frameTitle

local hideBtn = Instance.new("TextButton")
hideBtn.Size=UDim2.new(0,22.5,0,22.5)
hideBtn.Position=UDim2.new(0.82,0,0,0)
hideBtn.BackgroundColor3=Color3.fromRGB(0, 170, 255)
hideBtn.TextColor=BrickColor.new("White")
hideBtn.Text="–"
hideBtn.Parent=frameTitle

iconFrame.MouseButton1Click:Connect(function()
	iconFrame.Visible=not iconFrame.Visible
	if iconFrame.Visible then 
		Mainframe.Visible=false
	else
		Mainframe.Position=iconFrame.Position
		Mainframe.Visible=true
	end
end)

hideBtn.MouseButton1Click:Connect(function()
	Mainframe.Visible=false
	iconFrame.Position=Mainframe.Position
	iconFrame.Visible=true
end)

destroyBtn.MouseButton1Click:Connect(function()
	screenGui:Destroy()
end)

local function txtBox(parent) 
	local box = Instance.new("TextBox")
	box.Size=UDim2.new(0.8, 0,0.4,0)
	box.Position=UDim2.new(0.1,0,0.4,0)
	box.BackgroundColor3=blk_color
	box.Text="Number"
	box.TextSize=5
	box.TextColor=BrickColor.new("White")
	box.Parent=parent
	return box

end

local function newBtn(name,pos,parent)
	local btn = Instance.new("TextButton")
	btn.Size=UDim2.new(0.9,0,0.06,0)
	btn.Position=UDim2.new(0.05,0,pos,0)
	btn.BackgroundColor3=Color3.fromRGB(36, 36, 36)
	btn.TextColor=BrickColor.new("White")
	btn.Text=name
	btn.TextSize=5
	btn.Parent=parent
	return btn
end

local function newLbl(name,pos,parent)
	local btn = Instance.new("TextLabel")
	btn.Size=UDim2.new(0.9,0,0.6,0)
	btn.Position=UDim2.new(0.05,0,pos,0)
	btn.BackgroundColor3=Color3.fromRGB(36, 36, 36)
	btn.TextColor=BrickColor.new("White")
	btn.Text=name
	btn.TextSize=5
	btn.Parent=parent
	return btn
end

local function newTrem(parent)
	local frame = Instance.new("ImageLabel")
	frame.Size=UDim2.new(0,15,0,15)
	frame.Position=UDim2.new(0.8,0,0.1,0)
	frame.BackgroundTransparency=1
	frame.Image="rbxassetid://9468220156"
	frame.Parent=parent
	return frame
end

local function newKnob(parent)
	local frame = Instance.new("Frame")
	frame.Size=UDim2.new(0,7.5,0,7.5)
	frame.Position=UDim2.new(0.1,0,0.9,-8)
	frame.BackgroundColor3=Color3.fromRGB(255, 255, 255)
	frame.Parent=parent
	return frame
end

local function newPlate(parent)
	local frame = Instance.new("Frame")
	frame.Size=UDim2.new(0,20,0,11)
	frame.Position=UDim2.new(0.75,0,0.29,0)
	frame.BackgroundColor3=Color3.fromRGB(0, 0, 0)
	frame.Parent=parent
	return frame
end

local function newsideBtn(name, pos)
	local btn=Instance.new("TextButton")
	btn.Size=UDim2.new(0.17,0,0.1,0)
	btn.Position=UDim2.new(0.01,0,pos)
	btn.BackgroundColor3=gry
	btn.TextColor=BrickColor.new("White")
	btn.Text=name
	btn.TextScaled=true
	btn.Parent=Mainframe
	return btn
end

local function newBox(pos, parent)
	local box = Instance.new("TextBox")
	box.Size=UDim2.new(0.5,0,0.1,0)
	box.Position=UDim2.new(0,0,pos,0)
	box.BackgroundColor=BrickColor.new("Black")
	box.TextColor=BrickColor.new("White")
	box.TextScaled=true
	box.Text="Number"
	box.Parent=parent
	return box
end

local basicFrame = Instance.new("Frame")
basicFrame.Size=UDim2.new(0.4,0,0.3,0)
basicFrame.Position=UDim2.new(0.55,0,0.1,0)
basicFrame.BackgroundColor3=Color3.fromRGB(36, 36, 36)
basicFrame.Parent=Mainframe

local basicTitle = Instance.new("TextLabel")
basicTitle.Size=UDim2.new(0.8,0,0.1,0)
basicTitle.Position=UDim2.new(0.1,0,0.05,0)
basicTitle.BackgroundTransparency=1
basicTitle.TextColor=BrickColor.new("White")
basicTitle.Text="Player"
basicTitle.Parent=basicFrame

local remoteFrame = Instance.new("Frame")
remoteFrame.Size=UDim2.new(0.45,0,0.55,0)
remoteFrame.Position=UDim2.new(0.05,0,0.375,0)
remoteFrame.BackgroundColor3=Color3.fromRGB(36, 36, 36)
remoteFrame.Parent=Mainframe

local remoteTitle = Instance.new("TextLabel")
remoteTitle.Size=UDim2.new(0.8,0,0.1,0)
remoteTitle.Position=UDim2.new(0.1,0,0.04,0)
remoteTitle.BackgroundTransparency=1
remoteTitle.TextColor=BrickColor.new("White")
remoteTitle.Text="Remote"
remoteTitle.Parent=remoteFrame

local extraFrame = Instance.new("Frame")
extraFrame.Size=UDim2.new(0.4,0,0.4,0)
extraFrame.Position=UDim2.new(0.55,0,0.425,0)
extraFrame.BackgroundColor3=Color3.fromRGB(36, 36, 36)
extraFrame.Parent=Mainframe

local extraTitle = Instance.new("TextLabel")
extraTitle.Size=UDim2.new(0.8,0,0.1,0)
extraTitle.Position=UDim2.new(0.1,0,0.05,0)
extraTitle.BackgroundTransparency=1
extraTitle.TextColor=BrickColor.new("White")
extraTitle.Text="Extra"
extraTitle.Parent=extraFrame

local upState = false
local upBtn = newBtn(" Status Enabled",0.01,Mainframe)
upBtn.Size=UDim2.new(0.45,0,0.1,0)
upBtn.Position=UDim2.new(0.05,0,0.1,0)
upBtn.TextXAlignment="Left"
local upPlate = newPlate(upBtn)
local upTerm = newKnob(upPlate)

local speedSlideBtn = newLbl("Speed",0.08,Mainframe)
speedSlideBtn.Size=UDim2.new(0.225,0,0.12,0)
speedSlideBtn.Position=UDim2.new(0.05,0,0.225,0)
speedSlideBtn.TextYAlignment=Enum.TextYAlignment.Top
speedSlideBtn.Visible=false
local speedBox = txtBox(speedSlideBtn)
speedBox.Text=22

local jumpBtn = newLbl("Jump",0.08,Mainframe)
jumpBtn.Size=UDim2.new(0.225,0,0.12,0)
jumpBtn.Position=UDim2.new(0.3,0,0.225,0)
jumpBtn.TextYAlignment=Enum.TextYAlignment.Top
jumpBtn.Visible=false
local jumpBox = txtBox(jumpBtn)
jumpBox.Text=55

local noClipState = false
local noClipBtn = newBtn("  No Clip",0.15,basicFrame)
noClipBtn.Size=UDim2.new(0.9,0,0.2,0)
noClipBtn.Position=UDim2.new(0.05,0,0.2,0)
noClipBtn.TextXAlignment="Left"
local noClipPlate = newPlate(noClipBtn)
noClipPlate.Size=UDim2.new(0,18,0,8)
noClipPlate.Position=UDim2.new(0.7,0,0.29,0)
local noClipKnob = newKnob(noClipPlate)
noClipKnob.Size=UDim2.new(0,5,0,5)
noClipKnob.Position=UDim2.new(0.1,0,0.9,-6)

local moveState = false
local moveBtn = newBtn(" Move Enabled",0.22,basicFrame)
moveBtn.Size=UDim2.new(0.9,0,0.2,0)
moveBtn.Position=UDim2.new(0.05,0,0.45,0)
moveBtn.TextXAlignment="Left"
local movePlate = newPlate(moveBtn)
movePlate.Size=UDim2.new(0,18,0,8)
movePlate.Position=UDim2.new(0.7,0,0.29,0)
local moveKnob = newKnob(movePlate)
moveKnob.Size=UDim2.new(0,5,0,5)
moveKnob.Position=UDim2.new(0.1,0,0.9,-6)

local antiLavaState = false
local antiLavaBtn = newBtn(" Anti Lava",0.29,remoteFrame)
antiLavaBtn.Size=UDim2.new(0.9,0,0.175,0)
antiLavaBtn.Position=UDim2.new(0.05,0,0.525,0)
antiLavaBtn.TextXAlignment="Left"
local antiLavaPlate = newPlate(antiLavaBtn)
antiLavaPlate.Size=UDim2.new(0,20,0,9)
antiLavaPlate.Position=UDim2.new(0.75,0,0.29,0)
local antiLavaKnob = newKnob(antiLavaPlate)
antiLavaKnob.Size=UDim2.new(0,5,0,5)
antiLavaKnob.Position=UDim2.new(0.1,0,0.9,-7)

local anitBackState = false
local anitBackBtn = newBtn(" Anit Step-Back",0.36,remoteFrame)
anitBackBtn.Size=UDim2.new(0.9,0,0.175,0)
anitBackBtn.Position=UDim2.new(0.05,0,0.7,0)
anitBackBtn.TextXAlignment="Left"
local anitBackPlate = newPlate(anitBackBtn)
anitBackPlate.Size=UDim2.new(0,20,0,9)
anitBackPlate.Position=UDim2.new(0.75,0,0.29,0)
local anitBackKnob = newKnob(anitBackPlate)
anitBackKnob.Size=UDim2.new(0,5,0,5)
anitBackKnob.Position=UDim2.new(0.1,0,0.9,-7)

local maxZoomState = false
local maxZoomBtn = newBtn(" Max-Zoom",0.43,basicFrame)
maxZoomBtn.Size=UDim2.new(0.9,0,0.2,0)
maxZoomBtn.Position=UDim2.new(0.05,0,0.7,0)
maxZoomBtn.TextXAlignment="Left"
local maxZoomPlate = newPlate(maxZoomBtn)
maxZoomPlate.Size=UDim2.new(0,18,0,8)
maxZoomPlate.Position=UDim2.new(0.7,0,0.29,0)
local maxZoomKnob = newKnob(maxZoomPlate)
maxZoomKnob.Size=UDim2.new(0,5,0,5)
maxZoomKnob.Position=UDim2.new(0.1,0,0.9,-6)

local infEggState = false
local infEggBtn = newBtn(" Infinity Eggs",0.50,extraFrame)
infEggBtn.Size=UDim2.new(0.9,0,0.2,0)
infEggBtn.Position=UDim2.new(0.05,0,0.2,0)
infEggBtn.TextXAlignment="Left"
local infEggPlate = newPlate(infEggBtn)
infEggPlate.Size=UDim2.new(0,20,0,9)
infEggPlate.Position=UDim2.new(0.7,0,0.29,0)
local infEggKnob = newKnob(infEggPlate)
infEggKnob.Size=UDim2.new(0,6,0,6)
infEggKnob.Position=UDim2.new(0.1,0,0.9,-7)

local infCoinState = false
local infCoinBtn = newBtn(" Infinity Coins",0.50,extraFrame)
infCoinBtn.Size=UDim2.new(0.9,0,0.2,0)
infCoinBtn.Position=UDim2.new(0.05,0,0.45,0)
infCoinBtn.TextXAlignment="Left"
local infCoinPlate = newPlate(infCoinBtn)
infCoinPlate.Size=UDim2.new(0,20,0,9)
infCoinPlate.Position=UDim2.new(0.7,0,0.29,0)
local infCoinKnob = newKnob(infCoinPlate)
infCoinKnob.Size=UDim2.new(0,6,0,6)
infCoinKnob.Position=UDim2.new(0.1,0,0.9,-7)

local catchBtn = newBtn(" Catch Egg",0.05,remoteFrame)
catchBtn.Size=UDim2.new(0.9,0,0.15,0)
catchBtn.Position=UDim2.new(0.05,0,0.15,0)
catchBtn.TextXAlignment="Left"

local catchBtnTerm = newTrem(catchBtn)

local throwBtn = newBtn(" Perfect Throw💯",0.20,remoteFrame)
throwBtn.Size=UDim2.new(0.9,0,0.15,0)
throwBtn.Position=UDim2.new(0.05,0,0.35,0)
throwBtn.TextXAlignment="Left"
local throwBtnTerm = newTrem(throwBtn)

local reBtn = newBtn(" Rest Chaarcter",0.35,extraFrame)
reBtn.Size=UDim2.new(0.9,0,0.15,0)
reBtn.Position=UDim2.new(0.05,0,0.7,0)
reBtn.TextXAlignment="Left"

local reTerm = newTrem(reBtn)


local function newUi(parent) 
	local ui = Instance.new("UICorner")
	ui.CornerRadius = UDim.new(0,10)
	ui.Parent=parent
	return ui
end

local function newCustomUi(parent, rb,lb,ru,lu) 
	local ui = Instance.new("UICorner")
	ui.BottomLeftRadius =UDim.new(0,lb)
	ui.BottomRightRadius=UDim.new(0,rb)
	ui.TopLeftRadius=UDim.new(0,lu)
	ui.TopRightRadius=UDim.new(0,ru)
	ui.Parent=parent
	return ui
end

newUi(destroyBtn)
newUi(hideBtn)

newUi(Mainframe)
newCustomUi(frameTitle,0,0,10,10)
newUi(iconFrame)

newUi(basicFrame)
newUi(remoteFrame)
newUi(extraFrame)

newUi(noClipBtn)
newUi(noClipPlate)
newUi(noClipKnob)

newUi(moveBtn)
newUi(movePlate)
newUi(moveKnob)

newUi(upBtn)
newUi(upPlate)
newUi(upTerm)

newUi(speedSlideBtn)
newUi(speedBox)

newUi(jumpBtn)
newUi(jumpBox)

newUi(antiLavaBtn)
newUi(antiLavaPlate)
newUi(antiLavaKnob)

newUi(anitBackBtn)
newUi(anitBackPlate)
newUi(anitBackKnob)

newUi(maxZoomBtn)
newUi(maxZoomPlate)
newUi(maxZoomKnob)

newUi(infEggBtn)
newUi(infEggPlate)
newUi(infEggKnob)

newUi(catchBtn)
newUi(throwBtn)
newUi(reBtn)

newUi(reTerm)
newUi(infCoinBtn)
newUi(infCoinKnob)
newUi(infCoinPlate)

local PlayerModule = require(LocalPlayer:WaitForChild("PlayerScripts"):WaitForChild("PlayerModule"))
local Controls = PlayerModule:GetControls()

local function forceDynamicControl()
	-- 1. فرض نمط الحركة الديناميكية
	LocalPlayer.DevTouchMovementMode = Enum.DevTouchMovementMode.DynamicThumbstick

	-- 2. إجبار نظام التحكم البرمجي على التفعيل (Enable)
	if Controls then
		Controls:Enable(true)
	end

	-- 3. إظهار واجهات اللمس وإعادة تفعيلها في PlayerGui
	local touchGui = PlayerGui:FindFirstChild("TouchGui")
	if touchGui then
		touchGui.Enabled = true
		local touchControlFrame = touchGui:FindFirstChild("TouchControlFrame")
		if touchControlFrame then
			touchControlFrame.Visible = true

			local dynamicThumbstick = touchControlFrame:FindFirstChild("DynamicThumbstickFrame")
			if dynamicThumbstick then
				dynamicThumbstick.Visible = true
			end
		end
	end
end

runService.Stepped:Connect(function()
	if noClipState and LocalPlayer.Character then
		if LocalPlayer.Character:FindFirstChild("Humanoid") and not LocalPlayer.Character.Humanoid.Sit then
			for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
				if part:IsA("BasePart") then
					part.CanCollide = false
				end
			end
		end
	end
	if moveState then
		forceDynamicControl()
	end
	if antiLavaState then 
		local lava = workspace:FindFirstChild("lava") or workspace:FindFirstChild("Lava")
		if lava then lava.CanTouch=false else lava.CanTouch=true end
	end
end)

runService.Heartbeat:Connect(function()
	if upState then 
		speedSlideBtn.Visible,jumpBtn.Visible=true,true
		local human = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
		if human then 
			if tonumber(speedBox.Text) then 
				human.WalkSpeed=speedBox.Text
			end
			if tonumber(jumpBox.Text) then 
				human.UseJumpPower=true
				human.JumpPower=jumpBox.Text
			end
		end
	end
	if maxZoomState then
		LocalPlayer.CameraMaxZoomDistance = 100000

		LocalPlayer.CameraMinZoomDistance = 0.5
	else
		LocalPlayer.CameraMaxZoomDistance = 128
		LocalPlayer.CameraMinZoomDistance = 10
	end
end)

-- 2. دالة تحديث حالة الريموت
local function updateStepBackState(enabled)
	anitBackState = enabled

	-- تعطيل أو تفعيل جميع الـ Connections المربوطة بالحدث
	if getconnections then
		for _, connection in ipairs(getconnections(StepBackEvent.OnClientEvent)) do
			if enabled then
				connection:Disable() -- حظر الريموت
			else
				connection:Enable()  -- إرجاع الريموت للعمل الطبيعي
			end
		end
	end
end

eggvalue:GetPropertyChangedSignal("Value"):Connect(function()
	if infEggState then eggvalue.Value=99 end
end)

coinValue:GetPropertyChangedSignal("Value"):Connect(function()
	if infCoinState then coinValue.Value=99999 end
end)

-- 5. حماية إضافية: في حال قام الماب بربط اتصال جديد أثناء اللعب
StepBackEvent.OnClientEvent:Connect(function()
	if anitBackState then
		updateStepBackState(true)
	end
end)

noClipBtn.MouseButton1Click:Connect(function()
	noClipState = not noClipState
	if noClipState then 
		noClipKnob.Position = UDim2.new(0.6, 0, 0.9, -6)
		noClipPlate.BackgroundColor3 = Color3.fromRGB(0, 85, 255)
	else 
		noClipKnob.Position=UDim2.new(0.1,0,0.9,-6)
		noClipPlate.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	end
end)

moveBtn.MouseButton1Click:Connect(function()
	moveState = not moveState
	if moveState then 
		moveKnob.Position = UDim2.new(0.6, 0, 0.9, -6)
		movePlate.BackgroundColor3 = Color3.fromRGB(0, 85, 255)
	else 
		moveKnob.Position = UDim2.new(0.1,0,0.9,-6)
		movePlate.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	end
end)

upBtn.MouseButton1Click:Connect(function()
	upState = not upState
	if upState then 
		upTerm.Position = UDim2.new(0.6, 0, 0.9, -8)
		upPlate.BackgroundColor3 = Color3.fromRGB(0, 85, 255)
	else 
		speedSlideBtn.Visible,jumpBtn.Visible=false,false
		upTerm.Position = UDim2.new(0.1, 0, 0.9, -8)
		upPlate.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	end
end)

antiLavaBtn.MouseButton1Click:Connect(function()
	antiLavaState = not antiLavaState
	if antiLavaState then 
		antiLavaKnob.Position = UDim2.new(0.65, 0, 0.9, -6)
		antiLavaPlate.BackgroundColor3 = Color3.fromRGB(0, 85, 255)
	else 
		antiLavaKnob.Position = UDim2.new(0.1, 0, 0.9, -6)
		antiLavaPlate.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	end
end)

catchBtn.MouseButton1Click:Connect(function()
	didCatchEvent:FireServer()
end)

throwBtn.MouseButton1Click:Connect(function()
	eggThrowEvent:FireServer("perfect", false)
end)

reBtn.MouseButton1Click:Connect(function()
	local human = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
	if human then
		human.Health=0
	end
end)

anitBackBtn.MouseButton1Click:Connect(function()
	anitBackState = not anitBackState

	updateStepBackState(anitBackState)

	if anitBackState then 
		anitBackKnob.Position = UDim2.new(0.65, 0, 0.9, -6)
		anitBackPlate.BackgroundColor3 = Color3.fromRGB(0, 85, 255)
	else 
		anitBackKnob.Position = UDim2.new(0.1, 0, 0.9, -6)
		anitBackPlate.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	end
end)

maxZoomBtn.MouseButton1Click:Connect(function()
	maxZoomState = not maxZoomState
	if maxZoomState then 
		maxZoomKnob.Position = UDim2.new(0.6, 0, 0.9, -6)
		maxZoomPlate.BackgroundColor3 = Color3.fromRGB(0, 85, 255)
	else 
		maxZoomKnob.Position = UDim2.new(0.1, 0, 0.9, -6)
		maxZoomPlate.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	end
end)

infEggBtn.MouseButton1Click:Connect(function()
	infEggState = not infEggState
	if infEggState then 

		eggvalue.Value=99
		infEggKnob.Position = UDim2.new(0.6, 0, 0.9, -7)
		infEggPlate.BackgroundColor3 = Color3.fromRGB(0, 85, 255)
	else 
		infEggKnob.Position = UDim2.new(0.1, 0, 0.9, -7)
		infEggPlate.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	end
end)

infCoinBtn.MouseButton1Click:Connect(function()
	infCoinState = not infCoinState
	if infCoinState then 

		coinValue.Value=99999
		infCoinKnob.Position = UDim2.new(0.6, 0, 0.9, -7)
		infCoinPlate.BackgroundColor3 = Color3.fromRGB(0, 85, 255)
	else 
		infCoinKnob.Position = UDim2.new(0.1, 0, 0.9, -7)
		infCoinPlate.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	end
end)
