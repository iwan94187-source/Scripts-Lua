-- Get-Player Info
local players=game:GetService("Players")
local player=players.LocalPlayer
local playerGui = player.PlayerGui

local ff = Instance.new("Folder")
ff.Name="Upgrades"
ff.Parent=player

local UpFolder = player:WaitForChild("Upgrades")
local ballsFolder = nil

for _, folder in ipairs(workspace:GetChildren()) do 
	if folder:IsA("Folder") and folder.Name == "Lobby" then 
		local hasSubFolder = false

		-- فحص هل يحتوي على مجلدات فرعية؟
		for _, item in ipairs(folder:GetChildren()) do 
			if item:IsA("Folder") then
				hasSubFolder = true
				break
			end
		end

		-- إذا لم يكن يحتوي على مجلد فرعي، إذن هذا هو المجلد المطلوب!
		if not hasSubFolder then
			ballsFolder = folder
			break -- التوقف عن البحث في بقية الـ Workspace
		end
	end
end

print(ballsFolder) --> سيطبع كائن المجلد أو nil لو لم يطابق الشروط


if not ballsFolder then
	warn("⚠️ لم يتم العثور على مجلد الكرات Folder'Ball'")
	return
end

local runService=game:GetService("RunService")

local screenGui = Instance.new("ScreenGui")
screenGui.ResetOnSpawn=false
screenGui.Parent=playerGui

local HITBOX_RADIUS = 30       -- نطاق منطقة الحماية (Hitbox) من مركز الشخصية
local PUSH_FORCE = 550        -- قوة الدفع (تزداد كلما اقتربت الكرة أكثر)
local MAX_ESCAPE_VELOCITY = 100 -- الحد الأقصى لسرعة الهروب لمنع الطيران اللانهائي

local function UIcorner(ui, lTop, rTop, lBtm, rBtm) 
	local uiCorner = Instance.new("UICorner")
	uiCorner.TopLeftRadius = UDim.new(0, rTop)
	uiCorner.TopRightRadius = UDim.new(0,lTop)
	uiCorner.BottomLeftRadius = UDim.new(0, rBtm)
	uiCorner.BottomRightRadius = UDim.new(0,lBtm)
	uiCorner.Parent = ui

	-- 4. إطار خارجي رمادي خفيف للتمييز (اختياري)
	local uiStroke = Instance.new("UIStroke")
	uiStroke.Color = Color3.fromRGB(40, 40, 40)
	uiStroke.Thickness = 1.5
	uiStroke.Parent = ui
	return uiCorner
end 

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 250, 0, 250)
frame.Position = UDim2.new(0.3, 0, 0.15, 0)
frame.BackgroundColor3 = Color3.fromRGB(24, 24, 24)
frame.Active = true
frame.Draggable = true -- يجعل النافذة قابلة للسحب!
frame.Parent = screenGui



local frameTitle = Instance.new("TextLabel")
frameTitle.Size=UDim2.new(1,0,0.1,0)
frameTitle.Text="Dodge or die v5.5"
frameTitle.TextXAlignment=Enum.TextXAlignment.Left
frameTitle.TextColor=BrickColor.new("White")
frameTitle.BackgroundColor3=Color3.fromRGB(24, 24, 24)
frameTitle.TextXAlignment=Enum.TextXAlignment.Left
frameTitle.TextScaled=true
frameTitle.Parent=frame

local IsOpen = true
local closeBtn = Instance.new("TextButton")
closeBtn.Size=UDim2.new(0,25,0,25)
closeBtn.Position=UDim2.new(0,0,0,0)
closeBtn.BackgroundColor=BrickColor.new("Dark grey")
closeBtn.Text="X"
closeBtn.TextColor=BrickColor.new("White")
closeBtn.Parent=screenGui

local function newBtn(name, pos)
	local btn = Instance.new("TextButton")
	btn.Size=UDim2.new(0.2,0,0.1,0)
	btn.Position=UDim2.new(0,0,pos,0)
	btn.Text=name
	btn.TextScaled=true
	btn.TextColor=BrickColor.new("White")
	btn.BackgroundColor=BrickColor.new("Dark grey")
	btn.Parent=frame
	return btn
end

local function newScrollFrame()
	local scroll = Instance.new("ScrollingFrame")
	scroll.Size=UDim2.new(0.8,0,0.9,0)
	scroll.Position=UDim2.new(0.2,0,0.1,0)
	scroll.BackgroundColor3=Color3.new(0.24,0.24,0.24)
	scroll.Visible=false
	scroll.Parent=frame
	return scroll
end

local upgradeBtn = newBtn("Upgrades", 0.1)
local upgradeScroll=newScrollFrame()

local unBtn = newBtn("Universal", 0.2)
local unScroll=newScrollFrame()

UIcorner(frame, 10,10,10,10)
UIcorner(frameTitle, 10,10,0,0)
UIcorner(upgradeScroll, 0,0,10,0)
UIcorner(unScroll, 0,0,10,0)

local function newallBtn(name, pos, parent)
	local btn = Instance.new("TextButton")
	btn.Size=UDim2.new(0.3,0,0.05,0)
	btn.Position=UDim2.new(0,0,pos,0)
	btn.Text=name
	btn.TextSize=10
	btn.TextColor=BrickColor.new("White")
	btn.BackgroundColor=BrickColor.new("Really red")

	btn.Parent=parent
	return btn
end

local function newBox(pos, parent)
	local txt = Instance.new("TextBox")
	txt.Size=UDim2.new(0.7,0,0.05,0)
	txt.Position=UDim2.new(0.3,0,pos,0)
	txt.Text="Number"
	txt.TextSize=10
	txt.TextColor=BrickColor.new("White")
	txt.BackgroundColor3=Color3.new(0.2, 0.2, 0.2)
	txt.Parent=parent
	return txt
end

local speedState=false
local speedBtn=newallBtn("Speed", 0, upgradeScroll)
local speedBox = newBox(0,upgradeScroll)

local dashState = false
local dashBtn=newallBtn("Dash", 0.05, upgradeScroll)
local dashBox = newBox(0.05,upgradeScroll)

local jumpState=false
local jumpBtn=newallBtn("Jump", 0.11, upgradeScroll)
local jumpBox = newBox(0.11,upgradeScroll)

local function newBtn2(name, pos, parent)
	local btn = Instance.new("TextButton")
	btn.Size=UDim2.new(1,0,0.05,0)
	btn.Position=UDim2.new(0,0,pos,0)
	btn.Text=name
	btn.TextSize=10
	btn.TextColor=BrickColor.new("White")
	btn.BackgroundColor=BrickColor.new("Royal purple")
	btn.TextXAlignment="Left"
	btn.Parent=parent
	return btn
end

local function newTerm(parent)
	local term = Instance.new("TextLabel")
	term.Size = UDim2.new(0,25,1,0)
	term.Position=UDim2.new(0.8,0,0,0)
	term.BackgroundTransparency=1
	term.Text="🔴"
	term.TextSize = 10
	term.Parent=parent
	return term
end

local orbState = false
local orbBtn = newBtn2("Dodge Aura", 0, unScroll)
local orbTrem = newTerm(orbBtn)

local voidState = false
local voidBtn = newBtn2("Void", 0.05, unScroll)
local voidTerm = newTerm(voidBtn)

local invState = false
local invBtn = newBtn2("Invisible", 0.1, unScroll)
local invTerm = newTerm(invBtn)

local safeLineState = false
local safeLineBtn = newBtn2("Dodge Aura", 0.15, unScroll)
local safeLineTerm = newTerm(safeLineBtn)

local anitkillPartState=false
local anitkillPartBtn = newBtn2("Anit-Lava/Lazer", 0.2, unScroll)
local anitkillPartTerm = newTerm(anitkillPartBtn)

local autoDodgeState =false
local autoDodgeBtn = newBtn2("Auto Dodge", 0.25, unScroll)
local autoDodgeTerm = newTerm(autoDodgeBtn)


closeBtn.MouseButton1Click:Connect(function()
	IsOpen=not IsOpen
	frame.Visible=IsOpen
	if IsOpen then closeBtn.Text="o"
	else closeBtn.Text="x"
	end
end)

local function displayBtn()
	upgradeScroll.Visible=false
	unScroll.Visible=false
end
upgradeBtn.MouseButton1Click:Connect(function()
	displayBtn()
	upgradeScroll.Visible=true
end)

speedBtn.MouseButton1Click:Connect(function()
	speedState=not speedState
	if speedState then speedBtn.BackgroundColor=BrickColor.new("Bright green")
	else speedBtn.BackgroundColor=BrickColor.new("Really red")	
	end
end)

dashBtn.MouseButton1Click:Connect(function()
	dashState=not dashState
	if dashState then dashBtn.BackgroundColor=BrickColor.new("Bright green")
	else dashBtn.BackgroundColor=BrickColor.new("Really red")	
	end
end)

jumpBtn.MouseButton1Click:Connect(function()
	jumpState=not jumpState
	if jumpState then jumpBtn.BackgroundColor=BrickColor.new("Bright green")
	else jumpBtn.BackgroundColor=BrickColor.new("Really red")	
	end
end)

unBtn.MouseButton1Click:Connect(function()
	displayBtn()
	unScroll.Visible=true
end)

orbBtn.MouseButton1Click:Connect(function()
	orbState=not orbState
	if orbState then orbTrem.Text="🟢"
	else orbTrem.Text="🔴"
	end
end)

voidBtn.MouseButton1Click:Connect(function()
	voidState=not voidState
	if voidState then voidTerm.Text="🟢"
	else voidTerm.Text="🔴"
	end
end)

safeLineBtn.MouseButton1Click:Connect(function()
	safeLineState=not safeLineState
	if safeLineState then 
		safeLineTerm.Text="🟢"
	else
		safeLineTerm.Text="🔴"
	end
end)

anitkillPartBtn.MouseButton1Click:Connect(function()
	anitkillPartState=not anitkillPartState
	if anitkillPartState then 
		anitkillPartTerm.Text="🟢"
	else
		anitkillPartTerm.Text="🔴"
	end
end)

invBtn.MouseButton1Click:Connect(function()
	invState=not invState
	if invState then 
		invTerm.Text="🟢"
	else 
		local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
		if hrp then hrp.Position = Vector3.new(-136,172,0.6) end
		invTerm.Text="🔴"
	end
end)

autoDodgeBtn.MouseButton1Click:Connect(function()
	autoDodgeState=not autoDodgeState
	if autoDodgeState then 
		autoDodgeTerm.Text="🟢"
	else 
		
		autoDodgeTerm.Text="🔴"
	end
end)



local function setupBallTrajectory(ball)
	if not ball:IsA("BasePart") then return end

	-- إنشاء مجسم الخط (Laser Line)
	local linePart = Instance.new("Part")
	linePart.Name = "TrajectoryLine"
	linePart.Anchored = true
	linePart.CanCollide = false
	linePart.Material = Enum.Material.ForceField
	linePart.Color = Color3.fromRGB(255, 50, 50) -- لون أحمر نيون
	linePart.Transparency = 1
	linePart.Size = Vector3.new(0.6, 0.6, 1)
	linePart.Parent = workspace

	local lineLength = 150 -- طول الخط
	local extraSafetyBuffer = 10 -- مسافة التأمين الإضافية بالـ Studs
	local updateConnection = nil

	updateConnection = runService.Heartbeat:Connect(function()
		if not ball or not ball.Parent or not ball:IsDescendantOf(workspace) then
			if updateConnection then updateConnection:Disconnect() end
			if linePart then linePart:Destroy() end
			return
		end

		-- 🧠 فحص شرط التفعيل الرئيسي هنا داخل Heartbeat
		if safeLineState then
			local velocity = ball.AssemblyLinearVelocity

			-- إذا كانت الكرة تتحرك
			if velocity.Magnitude > 1 then
				local direction = velocity.Unit

				-- ربط موقع واتجاه الخط بالكرة
				local lineCenter = ball.Position + (direction * (lineLength / 2))
				linePart.Size = Vector3.new(0.6, 0.6, lineLength)
				linePart.CFrame = CFrame.lookAt(lineCenter, lineCenter + direction)

				-- حساب مسافة الأمان أوتوماتيكياً
				local ballRadius = math.max(ball.Size.X, ball.Size.Y, ball.Size.Z) / 2
				local dynamicSafetyRadius = ballRadius + extraSafetyBuffer

				local lineCFrame = linePart.CFrame
				local lineForward = lineCFrame.LookVector

				-- فحص موقع كل لاعب بالنسبة لهالة الأمان
				for _, plr in ipairs(players:GetPlayers()) do
					local char = plr.Character
					local rootPart = char and char:FindFirstChild("HumanoidRootPart")
					local humanoid = char and char:FindFirstChildOfClass("Humanoid")

					if rootPart and humanoid and humanoid.Health > 0 then
						local playerPos = rootPart.Position
						local vecToPlayer = playerPos - lineCenter

						-- حساب المسافة الطولية والعمودية
						local forwardDist = math.abs(vecToPlayer:Dot(lineForward))
						local projection = vecToPlayer:Dot(lineForward) * lineForward
						local perpendicularVec = vecToPlayer - projection
						local perpendicularDist = perpendicularVec.Magnitude

						-- إذا دخل اللاعب في نطاق هالة الأمان
						if forwardDist <= (lineLength / 2) and perpendicularDist <= dynamicSafetyRadius then
							local pushDir = perpendicularDist > 0.1 and perpendicularVec.Unit or lineCFrame.RightVector

							local pushSpeed = 60
							local upSpeed = 15

							rootPart.AssemblyLinearVelocity = (pushDir * pushSpeed) + Vector3.new(0, upSpeed, 0)
						end
					end
				end
			else
				-- إخفاء الخط إذا كانت الكرة متوقفة
				linePart.Transparency = 1
			end
		else
			-- إذا كان safeLineState مساوياً لـ false، يتم إخفاء الخط وتجاهل الدفع
			linePart.Transparency = 1
		end
	end)

	-- تنظيف الخط عند حذف الكرة
	ball.Destroying:Connect(function()
		if updateConnection then updateConnection:Disconnect() end
		if linePart then linePart:Destroy() end
	end)
end

-- 3. تشغيل المراقبة للكرات الحالية والجديدة
for _, item in ipairs(ballsFolder:GetChildren()) do
	task.spawn(setupBallTrajectory, item)
end

ballsFolder.ChildAdded:Connect(function(child)
	task.spawn(setupBallTrajectory, child)
end)

runService.Heartbeat:Connect(function(deltaTime)
	if speedState then 
		if tonumber(speedBox.Text) then 
			local spd = UpFolder:FindFirstChild("Speed")
			if spd then 
				spd.Value=speedBox.Text
			end
		end
	end

	if dashState then 
		if tonumber(dashBox.Text) then 
			local dsh = UpFolder:FindFirstChild("Dashes")
			if dsh then 
				dsh.Value=dashBox.Text
			end
		end
	end

	if jumpState then 
		if tonumber(jumpBox.Text) then 
			local jp = UpFolder:FindFirstChild("Jump")
			if jp then 
				jp.Value=jumpBox.Text
			end
		end
	end

	if orbState then 
		local character = player.Character
		if not character then return end

		local hrp = character:FindFirstChild("HumanoidRootPart")
		local humanoid = character:FindFirstChild("Humanoid")

		if not hrp or not humanoid or humanoid.Health <= 0 then return end

		local totalPushVector = Vector3.zero
		local activeBallsCount = 0

		-- فحص جميع الكرات الموجودة داخل المجلد
		for _, ball in ipairs(ballsFolder:GetChildren()) do
			if ball:IsA("BasePart") then
				local distance = (hrp.Position - ball.Position).Magnitude

				-- إذا دخلت الكرة نطاق الـ Hitbox
				if distance < HITBOX_RADIUS and distance > 0 then
					activeBallsCount += 1

					-- حساب اتجاه الهروب بعيداً عن الكرة (Direction)
					local pushDirection = (hrp.Position - ball.Position).Unit

					-- حساب مقدار التداخل/الاقتراب (كلما اقتربت الكرة، زاد الدفع)
					local overlap = (HITBOX_RADIUS - distance) / HITBOX_RADIUS

					-- إضافة المتجه المحسوب إلى المتجه الإجمالي
					totalPushVector = totalPushVector + (pushDirection * (overlap * PUSH_FORCE))
				end
			end
		end

		-- تطبيق قوة الهروب إذا كانت هناك كرات قريبة
		if activeBallsCount > 0 and totalPushVector.Magnitude > 0 then
			-- دمج التأثير بشكل سلس دون إلغاء حركة اللاعب الأساسية
			local currentVelocity = hrp.AssemblyLinearVelocity

			-- تجميع دفع الكرات مع الحفاظ على الحركة الرأسية (القفز / الجاذبية)
			local targetVelocity = Vector3.new(
				currentVelocity.X + totalPushVector.X * deltaTime * 10,
				currentVelocity.Y, -- الحفاظ على الجاذبية طبيعية
				currentVelocity.Z + totalPushVector.Z * deltaTime * 10
			)

			-- تحديد حد أقصى للسرعة للحد من الطيران المفاجئ
			if targetVelocity.Magnitude > MAX_ESCAPE_VELOCITY then
				targetVelocity = targetVelocity.Unit * MAX_ESCAPE_VELOCITY
			end

			hrp.AssemblyLinearVelocity = targetVelocity
		end
	end

	if voidState then 
		local map = workspace:FindFirstChild("CurrentMap")
		if map then 
			for _, item in ipairs(map:GetChildren()) do 
				if item.Name == "Wall" or item.Name == "WALL" then 
					item.CanCollide=false
				end
			end
		end
	end

	if invState then 
		local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
		local map = workspace:FindFirstChild("CurrentMap")
		if hrp and map then 
			local ground = map:FindFirstChild("Baseplate")
			if ground then 
				hrp.CFrame=ground.CFrame + Vector3.new(0,-5,0)
			end
		end
	end

	if anitkillPartState then 
		local folderMap = workspace:FindFirstChild("CurrentMap")
		if folderMap then 
			local killPart = folderMap:FindFirstChild("Lava") or 
				folderMap:FindFirstChild("SpinningLazer") and folderMap:FindFirstChild("SpinningLazer"):FindFirstChild("Lazer")
			if killPart then 
				killPart:Destroy()
			end
		end
	end
end)
